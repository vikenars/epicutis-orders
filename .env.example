# ──────────────────────────────────────────────────────────────────────────────
# Epicutis Orders — environment variables
#
# Copy to .env (NEVER commit .env) and fill in real values for local dev.
# In production (Railway / Render), set these in the platform's secret store.
# Never hardcode secrets in source.
# ──────────────────────────────────────────────────────────────────────────────

NODE_ENV=development
PORT=5000

# ── Shopify Admin API ────────────────────────────────────────────────────────
# The store domain — e.g. "new-epicutis.myshopify.com" (no protocol).
SHOPIFY_STORE_DOMAIN=

# Preferred: a long-lived Shopify admin access token. If set, it is used
# directly for all admin GraphQL calls.
SHOPIFY_ADMIN_ACCESS_TOKEN=

# Alternative: public-app client credentials. Used only if
# SHOPIFY_ADMIN_ACCESS_TOKEN is empty. The server exchanges these for a
# short-lived access token via /admin/oauth/access_token.
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=

# Optional override for the Shopify Admin API version. Defaults to 2024-01.
# SHOPIFY_API_VERSION=2024-01

# ── Email-OTP authentication ─────────────────────────────────────────────────
# Resend API key for delivering 6-digit sign-in codes.
# REQUIRED in production. In development, leave empty to log codes to console.
RESEND_API_KEY=

# Optional: override the OTP "from" address (must be a verified Resend sender).
# OTP_FROM_ADDRESS=Epicutis Orders <onboarding@resend.dev>

# Optional: a reply-to header on OTP emails.
# OTP_REPLY_TO=

# ── Role-based access control ────────────────────────────────────────────────
# Admin/RSD see all orders. AEs see only orders whose salesperson matches one
# of their aliases in AE_SALESPERSONS. AEs not listed in AE_SALESPERSONS see
# zero orders (fail-closed) and the UI shows a "not configured" message.
#
#   ADMIN_EMAILS — comma-separated allowlist of admin emails. Admins see all
#   orders and the salesperson field. If unset, a built-in default list is
#   used (see server/auth.ts).
# ADMIN_EMAILS=alice@epicutis.com,bob@signumbio.com
#
#   RSD_EMAILS — comma-separated emails granted the RSD role. RSDs see all
#   orders and the salesperson field, same as admin.
# RSD_EMAILS=
#
#   AE_SALESPERSONS — REQUIRED for AE visibility. JSON object mapping each
#   AE's email (lowercased) to a list of aliases that match the salesperson
#   string written in the 4th pipe-delimited segment of Shopify order notes.
#   Matching is case-insensitive and whitespace-collapsed; substring matches
#   in either direction are accepted. AEs missing from this map see zero
#   orders.
# AE_SALESPERSONS={"jvillegas@epicutis.com":["Jose Villegas","JV"]}
